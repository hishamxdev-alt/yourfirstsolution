import { FullImagePathPipe } from './full-image-path.pipe';

describe('FullImagePathPipe', () => {
  it('create an instance', () => {
    const pipe = new FullImagePathPipe();
    expect(pipe).toBeTruthy();
  });
});
